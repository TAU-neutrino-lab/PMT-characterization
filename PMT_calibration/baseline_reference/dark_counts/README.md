Place the dark-count acquisition's voltage-specific median templates here.

The currently available raw baseline-reference files cover 800 V; rerun
`Baseline_reference_diagnostic.ipynb` to create `800V.npz`, then add the other
voltages before running the full sweep.
